---
import DraftBadge from '@/components/DraftBadge.astro'
import FormattedDate from '@/components/FormattedDate.astro'
import Icon from '@/components/Icon.astro'
import type { Post } from '@/lib/utils'

interface Props {
  post: Post
}

const { post } = Astro.props
const isDraft = post.data.draft && import.meta.env.DEV
---

<li>
  <a
    href={`/blog/${post.id}`}
    class="group -mx-3 grid grid-cols-[auto_minmax(0,1fr)_2rem] items-baseline gap-x-3 rounded-lg px-3 py-2 transition-colors duration-150 hover:bg-tint sm:grid-cols-[5.75rem_minmax(0,1fr)_5.75rem]"
  >
    <FormattedDate
      date={post.data.publishDate}
      options={{ month: 'short', day: 'numeric', year: 'numeric' }}
      class="col-start-1 row-start-1 whitespace-nowrap font-mono text-[0.72rem] tabular-nums text-quiet"
    />
    <span
      class="col-start-2 row-start-1 flex min-w-0 items-center justify-center gap-2 text-center text-[0.94rem] leading-snug text-ink transition-colors duration-150 group-hover:text-accent"
    >
      <span class="min-w-0 truncate">{post.data.title}</span>
      {isDraft && <DraftBadge />}
    </span>
    <Icon
      name="arrow-right"
      size={14}
      class="col-start-3 row-start-1 self-center justify-self-end text-quiet/65 transition-transform duration-200 group-hover:translate-x-0.5 group-hover:text-accent"
    />
  </a>
</li>
